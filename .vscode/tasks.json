{
    "version": "2.0.0",
    "tasks": [
        {
            "type":"shell",
            "command": "cargo run --release",
            "label" : "cargo run thing",
            "group": "build"
        }
    ]
}